//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ prefix }}{{ module_info.name }}ViewController: UIViewController {

	//MARK: - IBProperties

    @IBOutlet var tableView: {{ prefix }}{{ module_info.name }}TableView! {
        didSet {
            self.tableView.register(UINib(nibName: "{{ prefix }}{{ module_info.name }}TableViewCell", bundle: nil), forCellReuseIdentifier: "{{ prefix }}{{ module_info.name }}TableViewCellIdentifier")
            self.tableView.delegate = self
            self.tableView.dataSource = self
        }
    }
    
    //MARK: - Properties
    
    var viewModel: {{ prefix }}{{ module_info.name }}ViewModelProtocol?
    
    //MARK: - Life Cycle
    
    override func viewDidLoad() {
        super.viewDidLoad()
        self.mainSetup()
    }

	//MARK: - Functions
	
	private func mainSetup() {
		
	}
	
	private func addObservers() {
		self.viewModel?.mainTableViewDataArray.addObserver { [weak self] (mainTableViewDataArray, nil) in
			self?.tableView.mainTableViewDataArray = mainTableViewDataArray
			self?.tableView.reloadData()
	}

}
